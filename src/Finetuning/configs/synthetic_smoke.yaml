model:
  target_model_path: null
  torch_dtype: float32
  mask_token_id: 0
  num_draft_layers: 2
  block_size: 4
data:
  train_data_path: null
  hidden_states_path: null
  max_length: 16
  chat_template: qwen3
  feature_dtype: float32
training:
  strategy: dflash
  num_epochs: 1
  max_steps: 4
  batch_size: 1
  accumulation_steps: 1
  learning_rate: 0.03
  warmup_ratio: 0.0
  scheduler_type: cosine
  max_grad_norm: 1.0
  num_anchors: 2
  loss_decay_gamma: null
  objective_chunk_blocks: 0
  attention_backend: eager
  loss_type: dflash
  dpace_alpha: 0.5
  save_interval: 1
  eval_interval: 1
  log_interval: 1
  seed: 42
output_dir: outputs/finetuning-synthetic
run_id: synthetic
device: cpu
offline: true
synthetic: true
